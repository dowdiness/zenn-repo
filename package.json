{
  "name": "GitVault",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "zenn": "zenn",
    "zenn:preview": "zenn preview",
    "zenn:build": "zenn build",
    "zenn:deploy": "zenn deploy",
    "zenn:deploy:preview": "zenn deploy --preview",
    "zenn:deploy:force": "zenn deploy --force",
    "zenn:deploy:force:preview": "zenn deploy --force --preview",
    "zenn:deploy:force:preview:dry-run": "zenn deploy --force --preview --dry-run",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "zenn-cli": "^0.1.154"
  },
  "devDependencies": {
    "textlint": "^14.0.4",
    "textlint-rule-preset-ja-spacing": "^2.4.3",
    "textlint-rule-preset-ja-technical-writing": "^10.0.1",
    "textlint-rule-spellcheck-tech-word": "^5.0.0"
  }
}
